import { ReconcilePanel } from "@/components/reconcile/reconcile-panel";

export const metadata = { title: "Reconcile · HTP" };

export default function ReconcilePage() {
  return <ReconcilePanel />;
}
