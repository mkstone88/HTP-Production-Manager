import { ScorecardTable } from "@/components/analytics/scorecard-table";

export const metadata = { title: "Scorecard · HTP" };

export default function ScorecardPage() {
  return <ScorecardTable />;
}
